let h5= document.querySelectorAll('.titulo');//seleccionamos el nodo que queremos cambiar

h5.forEach(element => { //indicamos con la funcion que todos los valores "titulo" cambien color.
    
   element.style.color= "blue"; 
});
 
 

 
